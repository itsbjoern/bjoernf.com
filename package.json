{
  "scripts": {
    "start": "docker-compose -f docker-compose.yml -f docker/dev-config.yml -f docker/secret.yml up --build",
    "build": "docker compose rm -f; docker volume prune -f; docker compose build --no-cache"
  }
}
