---
interface Props {
  icon: string;
  class?: string;
}

const { icon, class: _class, ...rest } = Astro.props;
---

<style>
  svg {
    user-select: none;
    width: 0.8em;
    height: 0.8em;
    font-size: 1em;
    display: inline-block;
    fill: currentColor;
    flex-shrink: 0;
    transition: none;
  }
</style>
<svg class:list={[_class]} {...rest}>
  <Fragment set:html={icon} />
</svg>
